description: |
  Install kops

usage:
  version: 2.1

  orbs:
    kubernetes: circleci/kubernetes@x.y.z

  jobs:
    build:
      macos:
        xcode: "10.3"
      steps:
        - checkout
        - kubernetes/install-kops
